name: Quality Gate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  checks: write
  actions: write

jobs:
  quality:
    uses: ./.github/workflows/reusable-quality-gates.yml
    secrets:
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}