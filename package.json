{
    "dependencies": {
        "@effect/sql-pg": "4.0.0-beta.103",
        "effect": "^4.0.0-beta.103"
    },
    "devDependencies": {
        "@types/bun": "^1.3.14",
        "typescript": "^7.0.2"
    },
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "bun test",
        "tracker": "bun run src/cli.ts tracker",
        "report": "bun run src/cli.ts report",
        "migrate": "bun run src/cli.ts migrate"
    }
}
